name: colossus_pt
description: "Colossus Personal Training App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0
  sign_in_with_apple: ^6.1.0
  provider: ^6.1.1
  flutter_svg: ^2.0.9
  sqflite: ^2.3.0
  path: ^1.8.3
  url_launcher: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
